
# Exportaciones del Analizador de rendimiento: son insumo de trabajo, pesan
# y se regeneran desde Power BI. El catálogo derivado sí se versiona.
capturas/*.json
!capturas/catalogo.json

# Credenciales locales: NUNCA se suben al repositorio.
.env

.DS_Store

# Configuración local del entorno de desarrollo
.claude/
